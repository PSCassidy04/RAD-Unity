using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BounceScript : MonoBehaviour

{
    internal static void bounce(float jumpForce, Vector3 vector3)
    {
        //Rigidbody BouncePlatformrb = GetComponent<Rigidbody>();
        //if (BouncePlatformrb != null)
        //{
        //    BouncePlatformrb.AddExplosionForce(explosionForce, explosionPosition, 5);
        //}
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
